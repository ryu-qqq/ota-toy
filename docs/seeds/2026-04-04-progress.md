# 진행상황 시드 — 2026-04-04

## 수행 내용

### 전략 레이어 실행
- PO 백로그 재검증: Outbox, Redis Story 추가, 설계 변경 반영, Story 번호 재정리
- PL 정합성 점검: 설계 산출물 간 교차 검증 수행, MAJOR 3건 발견 및 해결
- PM 산출물 감사: 13개 산출물 완성도 평가 (PASS 4, PARTIAL 4, FAIL 5), AUDIT-REQUEST 4건 도출
- PL 구현 로드맵 8단계 수립

### convention-guardian ArchUnit 보강
- 신규 규칙 3개 추가 (Instant.now() 직접 호출 금지, Enum displayName() 필수, jakarta.validation 의존 금지)
- 기존 9 + 신규 3 = 총 12개 ArchUnit 규칙 운영

### PL MAJOR 이슈 해결
- ERD에 BC별 Outbox 테이블 추가 (ReservationOutbox, SupplierOutbox)
- 금액 타입 Money → BigDecimal로 통일
- 컨벤션 문서에 pricing/location BC 분리 현행화

### 도메인 온보딩 리뷰
- code-reviewer: 41파일 검증, BLOCKER 3 / MAJOR 8 / MINOR 5, FIX-REQUEST 11건
- spec-reviewer: 29규칙 중 16개 표현, BLOCKER 3 / MAJOR 5 / MINOR 5, FIX-REQUEST 8건

## 기술적 문제 해결

### ERD Outbox 누락
- **문제**: PL 정합성 점검에서 ERD에 Outbox 테이블이 없음을 발견. Outbox 패턴을 설계에 명시했으나 ERD에 반영하지 않은 상태
- **해결**: BC별 전용 Outbox 테이블(ReservationOutbox, SupplierOutbox) 설계 후 ERD에 추가. 공통 1개 대신 BC별 분리한 이유는 소비 주기와 재처리 정책이 다르기 때문

### Money 타입 불일치
- **문제**: 설계 문서에서 금액을 int로 정의했으나, Supplier 통합 시 다통화(USD 소수점) 지원이 필요
- **해결**: 모든 금액 필드를 BigDecimal로 통일. DB 컬럼은 DECIMAL(19,4)로 설정하여 센트 이하까지 표현 가능

### 컨벤션 미현행화
- **문제**: pricing, location BC가 실제 설계에서 분리되었으나 컨벤션 문서에 반영되지 않은 상태
- **해결**: 컨벤션 문서에 pricing/location BC 분리를 현행화. 모듈 구조와 패키지 구조에 반영

### 도메인 코드 일관성 부족
- **문제**: VO 래핑 수준 불일치, compact constructor 패턴 불일치, Entity에 setter 존재
- **해결 방향**: Entity setter 전면 금지 + create() 팩토리 패턴으로 통일. marketplace 프로젝트의 persistence 구조를 참조하여 Entity 패턴 표준 수립

## 핵심 발견

### 긍정적
- 설계 방향(Hexagonal, BC 분리, Outbox 패턴)은 올바르게 잡혀 있음
- 6개 에이전트 파이프라인이 효과적으로 작동하여 수작업 대비 빠른 검증 완료

### 보완 필요
- **미구현 도메인**: Inventory, Reservation, Partner, Supplier — 핵심 비즈니스 로직이 아직 코드에 없음
- **공통 VO 부재**: Money, DateRange, Coordinate 등 반복 사용되는 값 객체 미정의
- **비즈니스 규칙 미표현**: 취소 기한 정책, BrandId nullable 정책 등 설계 문서에만 존재
- **일관성**: VO 래핑 수준, compact constructor 적용이 파일마다 다름

---

## [후반] 수행 내용

### domain-harness Accommodation BC 실행
- Phase 0(전제조건) → Phase 1(ArchUnit) → Phase 2(code-reviewer + spec-reviewer 병렬) → Phase 3(FIX 루프 Round 1) → Phase 4(test-designer 57개 테스트 통과)
- 하네스가 자동으로 빌드 → 리뷰 → 수정 → 테스트 전 과정을 실행
- FIX 루프 1회만에 리뷰 전체 PASS

### record → class 전환 (사람 리뷰 발견)
- domain-harness 완료 후 직접 리뷰에서 Long id를 가진 record 13개의 구조적 문제 발견
- 전부 class로 전환 결정 (Decision 10)
- 전환 과정에서 하위 엔티티 ID VO 적용 (Decision 11), Money VO 적용 (Decision 12), enum 전환 (Decision 13), 래핑 객체 도입 (Decision 14), BrandId nullable 유지 판정 (Decision 15)

## [후반] 기술적 문제 해결

### record의 reconstitute() 검증 문제
- **문제**: record의 compact constructor가 모든 생성 경로에서 실행되어, DB 복원(reconstitute) 시에도 비즈니스 검증이 돌아감. 규칙이 변경되면 과거 데이터 복원이 실패
- **해결**: 13개 하위 엔티티를 전부 class로 전환. forNew()에서만 검증, reconstitute()에서는 검증 없이 복원

### CONVENTION-DISPUTE: BrandId nullable
- **문제**: code-reviewer가 "참조 ID는 null 불허" 컨벤션으로 BLOCKER 지적
- **해결**: OTA 리서치 데이터(모텔 769개, 펜션 21개 등 대다수 브랜드 없음)를 근거로 nullable 유지 판정. 도메인 현실이 컨벤션보다 우선

## [후반] 핵심 발견

### 하네스의 한계와 사람 리뷰의 가치
- domain-harness가 57개 테스트를 통과시켰지만, record → class라는 구조적 설계 문제는 잡지 못함
- ArchUnit에 "Long id record 금지" 규칙이 없었기 때문. 리뷰어 에이전트도 이 관점의 체크리스트가 없어 놓침
- 자동화된 하네스는 유용하지만 만능이 아니며, 사람의 설계 리뷰가 여전히 필수라는 교훈
